# موقع مقامات الحريري

موقع ثابت (Static) لعرض مقامات الحريري باستخدام Jekyll، مع تصميم بسيط وجميل وعرض مميز للقصائد.

## المميزات

- ✨ عرض القصائد بشكل مرتب (شطرين متجاورين)
- 🎨 اختيار الخط العربي من 4 خطوط مختلفة
- 📱 تصميم متجاوب يعمل بشكل ممتاز على الجوال
- 🚀 سهولة إضافة مقامات جديدة
- 💾 حفظ الخط المختار في المتصفح

## الخطوط المتاحة

1. Amiri (افتراضي)
2. Scheherazade New
3. Lateef
4. Cairo

## طريقة الرفع على GitHub Pages

### 1. إنشاء مستودع جديد على GitHub
```bash
# إنشاء مستودع باسم: maqamat-hariri
# يمكنك تسميته أي شيء تريده
```

### 2. رفع الملفات
```bash
git init
git add .
git commit -m "أول رفع: موقع مقامات الحريري"
git branch -M main
git remote add origin https://github.com/username/maqamat-hariri.git
git push -u origin main
```

### 3. تفعيل GitHub Pages
1. اذهب إلى Settings في المستودع
2. اختر Pages من القائمة الجانبية
3. في Source، اختر `main` branch
4. احفظ التغييرات
5. الموقع سيكون متاحاً على: `https://username.github.io/maqamat-hariri`

## إضافة مقامة جديدة

1. أنشئ ملف جديد في مجلد `_maqamat` باسم مثل: `04-baghdadiya.md`
2. أضف المحتوى التالي:

```markdown
---
title: المقامة البغدادية
number: 4
---

محتوى المقامة هنا...

للقصائد، استخدم:

<div class="poem">
<div class="verse">
<div class="right-hemistich">الشطر الأول</div>
<div class="left-hemistich">الشطر الثاني</div>
</div>

<div class="verse">
<div class="right-hemistich">بيت آخر - شطر أول</div>
<div class="left-hemistich">بيت آخر - شطر ثاني</div>
</div>
</div>

باقي النص العادي...
```

## إضافة خط جديد

### 1. أضف الخط في ملف `assets/css/style.css`:

```css
/* في بداية الملف */
@import url('https://fonts.googleapis.com/css2?family=FONT_NAME:wght@400;700&display=swap');

/* في قسم الخطوط */
body.font-newfont { font-family: 'Font Name', serif; }
```

### 2. أضف خيار الخط في `_layouts/default.html`:

```html
<select id="fontSelect">
    <option value="amiri">Amiri</option>
    <option value="scheherazade">Scheherazade</option>
    <option value="lateef">Lateef</option>
    <option value="cairo">Cairo</option>
    <option value="newfont">الخط الجديد</option>
</select>
```

## تشغيل محلي للتجربة

```bash
# تثبيت Jekyll
gem install bundler jekyll

# إنشاء Gemfile
echo "source 'https://rubygems.org'" > Gemfile
echo "gem 'jekyll'" >> Gemfile

# تشغيل الموقع محلياً
bundle install
bundle exec jekyll serve

# افتح المتصفح على: http://localhost:4000
```

## هيكل المشروع

```
.
├── _config.yml           # إعدادات Jekyll
├── _layouts/             # قوالب الصفحات
│   ├── default.html      # القالب الأساسي
│   └── maqama.html       # قالب صفحة المقامة
├── _maqamat/             # مجلد المقامات
│   ├── 01-sanania.md
│   ├── 02-halawaniya.md
│   └── 03-dimashqiya.md
├── assets/               
│   ├── css/
│   │   └── style.css     # التنسيقات
│   └── js/
│       └── main.js       # كود JavaScript
├── index.html            # الصفحة الرئيسية
└── README.md             # هذا الملف
```

## ملاحظات مهمة

- القصائد تُعرض بشكل أفقي (شطرين) على الكمبيوتر
- على الجوال، القصائد تُعرض بشكل عمودي (شطر تحت شطر)
- الخط المختار يُحفظ تلقائياً في المتصفح
- يمكن كتابة المقامات بلغة Markdown العادية
- استخدم `<div class="poem">` لتنسيق القصائد

## التخصيص

يمكنك تخصيص:
- الألوان في ملف `style.css` (المتغيرات في `:root`)
- الخطوط عبر إضافة خطوط جديدة
- التصميم عبر تعديل ملفات CSS
- المحتوى عبر إضافة مقامات جديدة

## الترخيص

هذا المشروع مفتوح المصدر، استخدمه كما تشاء.
