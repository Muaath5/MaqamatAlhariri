# 🕌 موقع مقامات الحريري - دليل شامل

تم تصميم هذا الموقع خصيصاً لعرض مقامات الحريري بشكل جميل واحترافي مع التركيز على عرض القصائد.

---

## 📁 محتويات المشروع

```
maqamat-hariri/
├── _config.yml              # إعدادات Jekyll الأساسية
├── index.html               # الصفحة الرئيسية - قائمة المقامات
├── Gemfile                  # ملف تبعيات Ruby
├── .gitignore              # ملفات يتم تجاهلها في Git
│
├── _layouts/               # قوالب الصفحات
│   ├── default.html        # القالب الأساسي (Header + Footer + Font Selector)
│   └── maqama.html         # قالب صفحة المقامة الفردية
│
├── _maqamat/               # مجلد المقامات
│   ├── 00-template.md      # قالب لإضافة مقامة جديدة
│   ├── 01-sanania.md       # المقامة الصنعانية (مثال)
│   ├── 02-halawaniya.md    # المقامة الحلوانية (مثال)
│   └── 03-dimashqiya.md    # المقامة الدمشقية (مثال)
│
├── assets/
│   ├── css/
│   │   └── style.css       # جميع التنسيقات والخطوط
│   └── js/
│       └── main.js         # كود JavaScript (حفظ الخط)
│
└── README.md               # هذا الملف
```

---

## 🚀 الرفع على GitHub Pages

### الخطوات بالتفصيل:

#### 1️⃣ إنشاء حساب GitHub (إذا لم يكن لديك)
- اذهب إلى https://github.com
- سجل حساب جديد

#### 2️⃣ إنشاء مستودع جديد
1. اضغط على زر "+" في الزاوية ثم "New repository"
2. اسم المستودع: `maqamat-hariri` (أو أي اسم تريده)
3. اجعله **Public**
4. لا تضف README أو .gitignore (موجودة مسبقاً)
5. اضغط "Create repository"

#### 3️⃣ رفع الملفات
افتح Terminal أو Command Prompt في مجلد المشروع:

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "إضافة موقع مقامات الحريري"

# ربط المستودع
git branch -M main
git remote add origin https://github.com/اسمك/maqamat-hariri.git

# رفع الملفات
git push -u origin main
```

#### 4️⃣ تفعيل GitHub Pages
1. في صفحة المستودع على GitHub
2. اذهب إلى **Settings** (الإعدادات)
3. من القائمة الجانبية، اختر **Pages**
4. في قسم **Source**:
   - Branch: اختر `main`
   - Folder: اختر `/ (root)`
5. اضغط **Save**
6. انتظر 1-2 دقيقة
7. سيظهر رابط الموقع: `https://اسمك.github.io/maqamat-hariri`

---

## ✍️ إضافة مقامة جديدة

### الطريقة السهلة:

1. **انسخ الملف القالب:**
   ```bash
   cp _maqamat/00-template.md _maqamat/04-baghdadiya.md
   ```

2. **افتح الملف الجديد وعدّل:**
   ```markdown
   ---
   title: المقامة البغدادية
   number: 4
   ---

   حدّث الحارث بن همّام قال: ...
   ```

3. **للقصائد، استخدم هذا التنسيق:**
   ```html
   <div class="poem">
   <div class="verse">
   <div class="right-hemistich">الشطر الأول</div>
   <div class="left-hemistich">الشطر الثاني</div>
   </div>
   </div>
   ```

4. **احفظ ورفع:**
   ```bash
   git add .
   git commit -m "إضافة المقامة البغدادية"
   git push
   ```

---

## 🎨 إضافة خط عربي جديد

### مثال: إضافة خط Reem Kufi

#### 1️⃣ في ملف `assets/css/style.css`:

**أضف في بداية الملف:**
```css
@import url('https://fonts.googleapis.com/css2?family=Reem+Kufi:wght@400;700&display=swap');
```

**أضف في قسم الخطوط (بعد body.font-cairo):**
```css
body.font-reem { font-family: 'Reem Kufi', sans-serif; }
```

#### 2️⃣ في ملف `_layouts/default.html`:

**أضف خياراً جديداً في القائمة:**
```html
<select id="fontSelect">
    <option value="amiri">Amiri</option>
    <option value="scheherazade">Scheherazade</option>
    <option value="lateef">Lateef</option>
    <option value="cairo">Cairo</option>
    <option value="reem">Reem Kufi</option> <!-- جديد -->
</select>
```

#### 3️⃣ احفظ ورفع التغييرات:
```bash
git add .
git commit -m "إضافة خط Reem Kufi"
git push
```

### مصادر الخطوط العربية:
- Google Fonts: https://fonts.google.com/?subset=arabic
- Adobe Fonts: https://fonts.adobe.com/fonts

---

## 📱 التصميم المتجاوب

### على الكمبيوتر:
```
┌────────────────────────┬────────────────────────┐
│ إذا المرءُ لم يطلب    │  تركَّبَ في الدنيا   │
│ معالي أمره             │  ذليلاً مهانا         │
└────────────────────────┴────────────────────────┘
```

### على الجوال:
```
إذا المرءُ لم يطلب معالي أمره
تركَّبَ في الدنيا ذليلاً مهانا
```

الموقع يتكيف تلقائياً مع حجم الشاشة!

---

## 🎨 تخصيص الألوان

في ملف `assets/css/style.css`، عدّل المتغيرات في البداية:

```css
:root {
    --primary-color: #2c3e50;      /* لون أساسي */
    --secondary-color: #8b4513;    /* لون ثانوي (بني) */
    --background: #fdfaf6;         /* خلفية الصفحة */
    --card-bg: #fff;               /* خلفية البطاقات */
    --text-color: #2c3e50;         /* لون النص */
    --border-color: #d4af77;       /* لون الحدود (ذهبي) */
}
```

---

## 🔧 التشغيل المحلي (اختياري)

لاختبار الموقع على جهازك قبل الرفع:

```bash
# تثبيت Jekyll (مرة واحدة)
gem install bundler jekyll

# في مجلد المشروع
bundle install

# تشغيل الموقع
bundle exec jekyll serve

# افتح المتصفح على:
# http://localhost:4000
```

---

## ❓ الأسئلة الشائعة

### س: كيف أحذف مقامة؟
ج: احذف ملف المقامة من مجلد `_maqamat` ثم ارفع التغييرات.

### س: كيف أغير ترتيب المقامات؟
ج: غيّر رقم `number` في بداية كل ملف مقامة.

### س: الموقع لا يعمل بعد الرفع؟
ج: 
1. تأكد أن GitHub Pages مفعّل في Settings
2. انتظر 2-3 دقائق بعد الرفع
3. تأكد أن اسم branch هو `main` وليس `master`

### س: كيف أضيف صوراً؟
ج: 
1. ضع الصور في مجلد `assets/images/`
2. استخدم في المقامة: `![وصف الصورة]({{ '/assets/images/صورة.jpg' | relative_url }})`

### س: الخط لا يظهر بشكل صحيح؟
ج: تأكد من:
1. استيراد الخط في `style.css`
2. إضافة class في قسم الخطوط
3. إضافة option في `default.html`

---

## 📝 ملاحظات مهمة

✅ **احفظ الملفات بترميز UTF-8** للحفاظ على النصوص العربية

✅ **استخدم أسماء إنجليزية للملفات** (مثل: `01-sanania.md` وليس `01-صنعانية.md`)

✅ **ارفع بانتظام** لتجنب فقدان التعديلات

✅ **اختبر محلياً أولاً** إذا كنت تعمل على تعديلات كبيرة

✅ **القصائد تحتاج `<div class="poem">`** ليتم تنسيقها بشكل صحيح

❌ **لا تحذف** ملفات `_layouts` أو `assets` الأساسية

❌ **لا تعدّل** `_config.yml` إلا إذا كنت تعرف ما تفعل

---

## 📚 مصادر إضافية

- **Jekyll Documentation**: https://jekyllrb.com/docs/
- **GitHub Pages**: https://docs.github.com/en/pages
- **Markdown Guide**: https://www.markdownguide.org/
- **Google Fonts**: https://fonts.google.com/?subset=arabic

---

## 🎯 ملخص سريع

1. **إضافة مقامة**: انسخ `00-template.md` → عدّل → ارفع
2. **إضافة خط**: عدّل `style.css` + `default.html` → ارفع
3. **تغيير ألوان**: عدّل متغيرات `:root` في `style.css`
4. **الرفع**: `git add .` → `git commit -m "..."` → `git push`

---

بالتوفيق في موقعك! 🌟

إذا واجهت أي مشكلة، راجع ملف `QUICKSTART.md` أو `POEMS-GUIDE.md` للمزيد من التفاصيل.
